---
description: Conversational PRD intake + scaffold for a fresh project
---

Use the `bootstrap` skill from `.claude/skills/bootstrap/SKILL.md` to run the conversational PRD intake. Pre-flight check first: if `docs/PRD.md` already exists, ask whether to replace, replan, or leave alone. Otherwise begin the 7-question interview. Writes `docs/PRD.md`, `docs/local/completion-plan-<today>.md`, `docs/local/STATE.md`, `docs/sprint-loop.md`, and root `CLAUDE.md`. No code.
